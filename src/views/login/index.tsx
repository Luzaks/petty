import { Layout } from '../../shared';
import './index.css';

const LoginPage = ({}) => {
    return(
        <Layout>
            {(
            <div>Login</div>
            )}
        </Layout>
    );
};

export default LoginPage;