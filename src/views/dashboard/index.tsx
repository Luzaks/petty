import { Layout } from '../../shared';
import './index.css';

const DashboardPage = ({}) => {
    return(
        <Layout>
            {(
               <div>Dashboard</div>
            )}
        </Layout>
    );
};

export default DashboardPage;