import { ScrollContainer } from '../../shared';
import './index.css';

const DashboardPage = ({}) => {
    return(
        <ScrollContainer>
            {(
               <div>Dashboard</div>
            )}
        </ScrollContainer>
    );
};

export default DashboardPage;