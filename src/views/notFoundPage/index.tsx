import { ScrollContainer } from '../../shared';
import './index.css';

const NotFoundPage = ({}) => {
    return(
        <ScrollContainer>
            {(
                <div>Login</div>
            )}
        </ScrollContainer>
    );
};

export default NotFoundPage;