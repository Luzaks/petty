import { Layout } from '../../shared';
import './index.css';

const NotFoundPage = ({}) => {
    return(
        <Layout>
            {(
                <div>Login</div>
            )}
        </Layout>
    );
};

export default NotFoundPage;