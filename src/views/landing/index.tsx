import { Layout } from '../../shared';
import './index.css';

const LandingPage = ({}) => {
    return(
        <Layout>
            {(
               <div>Landing</div>
            )}
        </Layout>
    );
};

export default LandingPage;