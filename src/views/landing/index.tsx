import { ScrollContainer } from '../../shared';
import './index.css';

const LandingPage = ({}) => {
    return(
        <ScrollContainer>
            {(
               <div>Landing</div>
            )}
        </ScrollContainer>
    );
};

export default LandingPage;